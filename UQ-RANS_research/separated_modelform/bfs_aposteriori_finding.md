# Backward-facing-step model-form evidence (first geometry, a-posteriori)

Evidence package for the backward-facing-step leg of the separated-flow
model-form study: sampled realizable closures from the conditional generative
flow and from the two comparison baselines, propagated through the same
Reynolds-stress injection to predicted quantities of interest and measured
against the criterion of PRE_REGISTRATION.md as operationalized in
METHODS_OPERATIONALIZATION.md (all choices there were pinned before the
corresponding results were computed). The periodic-hills geometry and the
cross-geometry transfer clause are not yet run; this memo reports the first
geometry only and draws no conclusion about that clause.

Every number traces to `finding_numbers.json` in this directory, generated by
`python/UQ/reproduce_separated_apriori.py` and
`python/UQ/reproduce_separated_aposteriori.py` (fixed seed 0, production grid,
ensemble size 24, nominal level 0.90). Nothing was tuned toward any criterion.

## 1. Setup

Baseline: the corrected Le-Moin configuration (free-slip top boundary and
prescribed inflow boundary layer, both verified from the data; the
limiter-consistent Boussinesq anisotropy). The corrected baseline converges to
x_r/h = 6.611 against the DNS 6.28, an absolute error of 0.33 (5.3 percent).
The earlier uniform-inlet, no-slip-top setup gave 5.07 (19 percent low); most
of that gap was boundary-condition error, not closure error, so the model-form
correction now works against a baseline with little headroom on this quantity.

Injection: every method's sampled anisotropy enters the momentum equation
through the same explicit deferred-correction force with the realizability
projection applied before injection and re-checked every outer iteration.
Probabilistic ensemble members are coherent field realizations (one shared
latent per member); spatially white sampling appears only as the labeled
sensitivity diagnostic.

## 2. A-priori checkpoint (the pre-registered precursor)

In distribution (train on all six stations, score all points), the flow
reproduces its synthetic-validation behaviour on the real data and is better
than the Gaussian on every metric:

| model | coverage at 0.90 | CRPS | energy score | realizable fraction |
|---|---|---|---|---|
| generative flow | 0.948 | 0.0262 | 0.0868 | 1.000 |
| Gaussian model-form | 0.920 | 0.0275 | 0.0937 | 1.000 |

Leave-one-station-out (the pinned held-out unit), mean over components:

| held-out x/h | flow coverage | gauss coverage | flow CRPS | gauss CRPS |
|---|---|---|---|---|
| -3 | 0.546 | 0.656 | 0.0639 | 0.0651 |
| 4  | 0.909 | 0.773 | 0.0276 | 0.0277 |
| 6  | 0.737 | 0.784 | 0.0249 | 0.0238 |
| 10 | 0.844 | 0.933 | 0.0213 | 0.0234 |
| 15 | 0.862 | 0.875 | 0.0243 | 0.0260 |
| 19 | 0.804 | 0.925 | 0.0245 | 0.0264 |

Both families under-cover across held-out stations (flow mean 0.784, Gaussian
0.824), worst at the attached upstream station, which has no attached-flow
analog in the training set when held out: a genuine covariate shift, not a
fitting failure. The flow wins CRPS and the energy score in aggregate (0.0311
against 0.0321, and 0.1030 against 0.1081) and at four of six stations. Every
projected draw is realizable. Figure: `figures/bfs_apriori_coverage.png`.

## 3. A-posteriori result (24 coherent members per method, corner families)

Reattachment (truth 6.28; baseline point error 0.33):

| method | converged | 90 percent band / envelope | contains truth | ensemble-mean error | CRPS |
|---|---|---|---|---|---|
| generative flow | 23/24 | [6.50, 8.65] | NO | 1.01 | 0.651 |
| Gaussian model-form | 24/24 | [6.11, 7.70] | yes | 0.55 | 0.318 |
| eigenspace, Delta_B = 0.5 | 3/3 | [5.44, 8.04] | yes | (envelope) | 0.380 (uniform reading) |
| eigenspace, Delta_B = 1.0 | 2/3 | [4.94, 5.61] | NO | (envelope) | 0.839 (uniform reading) |

Wall skin friction at the five downstream stations:

| method | station coverage | CRPS | energy score |
|---|---|---|---|
| generative flow | 0.20 | 6.4e-4 | 1.5e-3 |
| Gaussian model-form | 0.40 | 4.5e-4 | 1.0e-3 |
| eigenspace, Delta_B = 0.5 | 0.60 | 3.9e-4 | 9.2e-4 |

Realizability in the running solve holds for every converged member of every
method (the barycentric check passes every outer iteration, zero violations).
One flow member and, at the full corner projection, the three-component
eigenspace member admit no steady solution under the coupled iteration; they
are counted and excluded per the pinned rule. The white-latent diagnostic
confirms the pinned expectation: independent-per-cell sampling under-disperses
(flow band [6.62, 7.30], Gaussian [6.67, 7.11]) and is not a usable band.
Figure: `figures/bfs_reattachment_intervals.png`.

## 4. Measured against the pre-registered criterion

The positive shape required all four clauses; on this geometry the generative
flow meets clause 3 and fails clauses 1 and 2:

1. Coverage and accuracy: FAILED for the flow. Its 90 percent band [6.50,
   8.65] does not contain 6.28 (short by 0.22 on the lower edge), and the
   ensemble-mean error (1.01) is larger than the baseline point error (0.33).
   The Gaussian band and the moderated eigenspace envelope do contain the
   truth, but neither reduces the point error either.
2. Proper scoring rules: FAILED for the flow. It is worse than BOTH baselines
   on reattachment CRPS and on the wall-friction scores.
3. Realizability in the running solve: PASSED for every method.
4. Cross-geometry generalization: NOT YET RUN (periodic hills pending).

This matches the pre-registered null or negative shape ("the generative
model-form does not beat the eigenspace-perturbation envelope on the proper
scores, or coverage is not restored") on the backward-facing step, and it is
reported as such.

Two measured facts qualify the reading without changing it:

- Both probabilistic ensembles shift the reattachment UP from the baseline
  (flow mean 7.29, Gaussian 6.83, baseline 6.61) while the truth lies BELOW
  the baseline (6.28). The training discrepancy is dominated by the attached,
  channel-like near-wall anisotropy error, and its coherent injection
  systematically lengthens the predicted bubble; the flow shifts further and
  spreads wider than the Gaussian (band width 2.15 against 1.59) yet still
  misses on the low side. The failure is directional, not a dispersion
  shortfall.
- The corrected baseline is only 5.3 percent off on this quantity, so the
  headroom the accuracy clause presumes is largely gone on this geometry; the
  criterion clauses were fixed in advance and are scored as written.

The calibration overlay (generalized Bayes and conformal, the standing
coverage-correction layer) has not been composed onto these ensembles, and the
second geometry has not been run; both belong to the remaining scope of the
study and neither changes what is measured above.

## 5. Reproduce

```
PYTHONPATH=build:python python3 python/UQ/reproduce_separated_apriori.py
PYTHONPATH=build:python python3 python/UQ/reproduce_separated_aposteriori.py --sensitivity
python3 UQ-RANS_research/separated_modelform/make_bfs_figures.py
```

Fixed seed 0 throughout; raw ensembles cache under the gitignored
`results/separated/`; the curated numbers here are `finding_numbers.json`.

## 6. Dated addendum (2026-07-02): pinned post-result diagnostics

Three diagnostics were requested on review of the result above; their protocols
were fixed in METHODS_OPERATIONALIZATION.md section 7 BEFORE any of them ran.
Numbers: the `diagnostics` block of `finding_numbers.json`. They attribute the
recorded result; none changes it.

### 6.1 Grid attribution of the baseline error

Same corrected boundary conditions and the same inlet profile (inlet_delta =
0.6784) at three resolutions:

| grid | reattachment x_r/h | delta_999 at x/h = -3 |
|---|---|---|
| 20+26 x 14+10 (coarse) | 5.716 | (coupled-test config) |
| 40+48 x 24+18 (production) | 6.611 | 1.186 |
| 60+72 x 36+27 (fine) | 6.877 | 0.933 |

The prediction is still moving under refinement (+0.90 then +0.27; apparent
order about 3, crude extrapolation to roughly 7.0). The same inlet develops a
thinner layer on the fine grid (0.933 against the matched 1.186; the inflow
match is resolution-dependent), so the fine number conflates resolution with an
inflow under-match and the pure-resolution shift is AT LEAST +0.27.

Consequence, stated against section 1: the production-grid baseline error
(+5.3 percent) UNDERSTATES the grid-converged closure error, which is an
over-prediction of roughly ten percent. The ensemble-against-baseline
comparisons of section 3 were run at the fixed production grid and stand as
recorded; any absolute statement about the closure carries a numerical
uncertainty of order 0.3 in x_r/h. Per the pinned protocol there is no further
refinement chasing; the attribution is the deliverable.

### 6.2 Region attribution of the ensemble shift, and a correction

The generative model's expected correction (per-cell conditional mean, 256
pointwise samples), injected on the production grid:

| injection mask | x_r/h | shift from baseline |
|---|---|---|
| full field | 6.905 | +0.294 |
| separated region only (0 <= x/h <= 10, y/h <= 1.5) | 6.923 | +0.312 |
| attached complement only | 6.630 | +0.020 |

This FALSIFIES the mechanism proposed in section 4: the attached-region
contribution is negligible, and the separated-region correction itself drives
the upward shift. Section 4's sentence attributing the displacement to the
attached, channel-like training component is withdrawn; the measured statement
is that the anisotropy discrepancy of the separated region, injected through
the momentum equation while the turbulence-transport equations keep their
baseline closure, lengthens the predicted bubble. A candidate mechanism
consistent with the literature (the conditioning concerns of Wu et al. 2019)
is that anisotropy-only injection is a partial substitution whose mean-flow
response need not move toward the DNS; it is stated here as untested. Also
measured: the expected-correction shift (+0.29) is under half the flow
ensemble's mean shift (+0.68), so the coupled response amplifies coherent
fluctuations asymmetrically.

### 6.3 Conformal calibration overlay (the standing coverage layer)

Calibrated on the five measured downstream wall-friction stations (never on
the reattachment being judged), scale-normalized split conformal at alpha =
0.10; with five calibration points the exact conformal quantile exceeds the
sample, so the reported bands are LOWER BOUNDS on the width the guarantee
requires:

| method | station scores | multiplier q | raw band | overlay band | contains truth |
|---|---|---|---|---|---|
| generative flow | 2.3, 2.8, 3.1, 6.9, 10.5 | 10.5 | [6.50, 8.65] (no) | [2.91, 11.12] | yes |
| Gaussian model-form | 1.1, 2.0, 2.7, 5.3, 7.9 | 7.9 | [6.11, 7.70] (yes) | [4.50, 9.18] | yes |

The overlay restores containment for both methods, and the price is the
finding: the wall-friction scores reveal both ensembles to be severely
overconfident on Cf (scores up to 10.5 where a calibrated ensemble would sit
near 1), so honest bands must be roughly five times wider than the raw ones,
at which width the reattachment interval spans much of the physically
plausible range and carries little information. Coverage is repairable by the
standing correction layer; sharpness is not, at this calibration-data
sparsity. The informative repair is a better-dispersed or better-directed
model-form rather than a wider multiplier, which is what the dense-field
second geometry tests.


## Post-audit note (2026-07-12)

See the dated post-audit revision at the end of hills_crossgeom_finding.md; it
applies to this memo's score tables and eigenspace naming too. For this
geometry specifically: the committed member records are CLEAN of the
stale-field defect (the max-iteration failure mode evaluates its own fields;
the bit-identical duplication was a hills DivergenceDetected artifact), and
the fair-score recomputation (fair_scores_recompute.json; the biased columns
reproduce the committed values exactly) moves the moderated three-corner
family from mid-pack to best-scoring on reattachment CRPS (0.380 biased to
0.092 fair at delta 0.5), strengthening the committed statement that the flow
does not beat the corner-family envelope.
