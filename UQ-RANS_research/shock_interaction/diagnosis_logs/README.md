Raw verbose logs of the 2026-07-20 coupled-round diagnosis probes (scripts
archived at python/UQ/probes/archive/). These are the evidence for the
confounded-round classification: identical stall masked and unmasked
(mask_ab), the failing-cell location and pre-step state (reject_probe),
the wedge surviving a raised mask floor (floor_probe) and a 6000-iteration
force ramp (ramp_probe), and the frozen-k variant removing the rejection
behavior while remaining unconverged (frozenk_probe). The turbulent
zero-correction identity defect that reclassified the round is repaired in
the stored-discrepancy injection; see the finding memo when adjudicated.
